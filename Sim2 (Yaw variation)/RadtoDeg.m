function output = RadtoDeg(input)
    phi     = input;     % Pitch angle
    theta   = input;     % Roll angle
    psi     = input;     % Yaw angle

    % Initializing the output vector
    output = [phi theta psi];
end
